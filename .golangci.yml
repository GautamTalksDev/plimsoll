version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - errcheck
    - gosec
    - govet
    - staticcheck
  exclusions:
    presets:
      - comments
      - std-error-handling
      - common-false-positives
    rules:
      # Tests intentionally exec binaries, write fixtures, and ignore some errors.
      - path: _test\.go
        linters: [errcheck, gosec]
      # The CLI opens caller-chosen paths by design.
      - path: ^(cmd/plimsoll|internal/(datasetload|sealfile|keys|evidence))/
        text: "G304"
        linters: [gosec]

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
