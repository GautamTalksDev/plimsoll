# Testdata

Fixtures for unit tests live here. Never a user's dataset, model, prompts, or outputs.

`canonical/` holds synthetic JSON for `internal/canonical`.
`seal/` holds synthetic pre-registration documents for `internal/seal`.
